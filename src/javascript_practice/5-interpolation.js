const username = 'Kenn';
console.log(`${username} is the true JS Wizard`);

console.log(`Hey students,
what bugs can ${username} fix for you today?`);

const person = {
    username2: 'Kenn',
    profession: 'Secret Agent'
};

console.log(`${person.username2}, ${person.profession}, extraordinaire`);